# Shared types

Cross-cutting types that don't belong to one domain module.

Empty for now: every type defined so far belongs clearly to one domain
(`curriculum/metadata`, `lessons`, `quizzes`, `playgrounds`, `simulations`,
`progress`) and lives there instead. Add a type here only once something
is genuinely shared across domains — don't move a type here speculatively.
