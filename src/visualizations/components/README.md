# Visualization components

Concrete, concept-facing visualizations (the knowledge graph, the neural
network diagram, workflow diagrams) built from `../primitives`.

Empty for now — see `../primitives/README.md` for why.
